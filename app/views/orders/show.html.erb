<%= stylesheet_link_tag "orders_index" %>
<h1>Your Cart</h1>
  <ul>
    <% @products.each do |product| %>
    <li class="span3">
      <div class="thumbnail">
        <div class="caption">
          <p><strong>Product Title</strong> <%= product.title %></p>
          <p><strong>Price</strong> <%= number_to_currency(product.price)  %></p>
          <p><strong>Quantity</strong> <%= product.quantity %></p>
          <!-- FIXME: hardcoded order & product id as 1 -->
          <!-- TODO: use the evenutal model method "@current_user.shopping_cart" to grab the current order id for the delete request -->
          <%= link_to 'Delete', orders_show_path, method: :delete, data: { confirm: 'Are you sure you want to delete this from your cart?' } %>

          <br/>
          
        </div>

      </div>
    </li>
  </ul>

<% end %>
<h2><strong>Total:</strong> </h2>
<a class="btn btn-success" href="#">Checkout</a>
<%= link_to "Back to main page", root_path %>
          
        </div>
      </div>
    </li>
  </ul>
  